import React from 'react';
import { Box } from 'grommet';
import ShipList from './ShipList';

const Home = (props) => {
  return (
    <Box>
      <ShipList />
    </Box>
  );
};

export default Home;
